spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: storage-service #服务名
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #nacos地址
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.61:3306/business?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: dspfzcqljt
server:
  port: 9001
dubbo:
  scan:
    # dubbo 服务扫描基准包
    base-packages: com.richye.sca.demo.storage.service.dubbo
  protocol:
    # dubbo 协议
    name: dubbo
    # dubbo 协议端口（ -1 表示自增端口，从 20880 开始）
    port: -1
  registry:
    # 挂载到 Spring Cloud 注册中心
    address: spring-cloud://localhost
mybatis:
  mapper-locations: classpath:/mapper/*.xml